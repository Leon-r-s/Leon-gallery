# サイトの使い方マニュアル

サイトのURL: https://leon-r-s.github.io/Leon-gallery/

困ったら、このファイルを開いて見てください。分からなくなったら、AIに聞いてもOKです。

---

## 共通の準備:「コマンドプロンプト」を開く

これから出てくる作業は、全部「コマンドプロンプト」という黒い画面で行います。

1. `C:\Leon\Fable5\marketing\kohaku-gallery-site` のフォルダを開く
2. 一番上の、住所が書いてある欄(アドレスバー)をクリックする
3. `cmd` と打って、Enterを押す
4. 黒い画面が開く(これが「コマンドプロンプト」)

**これから先の①②③…は、全部この黒い画面に文字を打って、Enterを押す作業です。**

---

## ① 新しい絵を追加したいとき

1. 絵を`images`フォルダに保存する。ファイル名は「日付_タイトル.jpg」の形にする
   例: `2026-08-29_おさんぽ.jpg`
2. コマンドプロンプトに、次のように打ってEnter

   ```
   更新する.bat
   ```

3. 文字が流れて、最後に「続行するには何かキーを押してください」と出たら、キーボードの何かキーを押す
4. GitHub Desktopを開く。変更されたファイルが並んでいるはず
5. 左下の「Summary」欄に、一言メモを書く(例:「絵を追加」)
6. 「Commit to master」ボタンを押す
7. 画面上部に出る「Push origin」ボタンを押す
8. 数十秒〜数分待ってから、サイトを確認する(反映されて見えなければ、`Ctrl + Shift + R`で強制的に読み込み直す)

---

## ② 作品が売れて、SoldOutにしたいとき

コマンドプロンプトに、次のように打ってEnter。**日付だけでOKです。**

```
node toggle.js 2026-08-15
```

これで、その日の作品が「販売中→SoldOut」に切り替わります。

**キャンセルが出て、また販売中に戻したいときも、まったく同じコマンドを、もう一度打つだけです。**(SoldOut→販売中に戻ります)

その後は、①と同じように、GitHub Desktopで **Commit → Push** してください。

---

## ③ BASEに登録した作品に、購入リンクをつけたいとき

コマンドプロンプトに、次のように打ってEnter。**日付とBASEのURL、両方を打ちます。**

```
node base-link.js 2026-08-15 https://leon-r-s.thebase.in/items/xxxxx
```

(`https://leon-r-s.thebase.in/items/xxxxx` の部分を、実際のBASEの商品URLに置き換えてください)

**リンクを消したいときは、URLを書かず、日付だけ打ちます。**

```
node base-link.js 2026-08-15
```

その後は、①と同じように、GitHub Desktopで **Commit → Push** してください。

---

## よくあること

- **サイトを見ても、反映されていないように見える** → ブラウザに古い状態が残っているだけのことが多いです。`Ctrl + Shift + R` を試してください
- **コマンドを打っても、エラーが出る** → エラーの文章を、そのままコピーして、AIに見せてください
- **`更新する.bat` をダブルクリックしたら、一瞬で消えた** → コマンドプロンプトから `更新する.bat` と打って実行すると、エラーが画面に残ります

---

## 販売を再開したいとき(ギャラリーだけの状態から戻す)

`script.js` というファイルの、一番上の方にある

```
const GALLERY_ONLY = true;
```

を

```
const GALLERY_ONLY = false;
```

に書き換えて、保存する。その後、GitHub Desktopで **Commit → Push**。

(この作業は、ファイルの中身を直接書き換えるので、不安なら、AIに頼んでください)
